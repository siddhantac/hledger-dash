# HTTPS URL of the git repo containing your hledger journal files.
# Include a personal access token for auth:
#   GitHub: Settings > Developer settings > Fine-grained tokens > contents: read
JOURNAL_REPO=https://<token>@github.com/you/journals.git

# Name of the main journal file inside the repo
JOURNAL_FILE=main.journal
